<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <span class="sobig"><%= @product.name %></span>
</p>

<p>
  <strong>Picture:</strong>
  <div class="wyno-image"><%= image_tag(@product.picture) %></div>
</p>

<p>
  <strong>Description:</strong>
  <span class="sobig"><%= @product.description %></span>
</p>

<p>
  <strong>Price:</strong>
  <span class="sobig">$<%= @product.formatted_price %></span>
</p>

<!-- <h3>Reviews</h3>
<% @product.reviews.each do |review| %>
  <p>
    <%= review.comment %>
    <br>
    Added on: <%= review.created_at %>
  </p>
<% end %>

<h3>New Review</h3>
<%= render 'reviews/form' %> -->
<Br>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
